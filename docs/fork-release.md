# MikroDash-cn container image

Pushes to this fork's `main` branch build multi-architecture images for
`linux/amd64`, `linux/arm64` and `linux/arm/v7` and publish:

```text
ghcr.io/invoker-karl/mikrodash-cn:latest
```

Version tags also publish versioned image tags through the inherited GitHub
Actions release workflow.
