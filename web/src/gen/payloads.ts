// GENERATED by cmd/tsgen — do not edit.
//
// The WebSocket payload types, read from the Go structs that produce them
// (internal/collect). Every property, its type and its optionality come from a
// json tag, so this file cannot drift from the wire: `go run ./cmd/tsgen -check`
// fails when it does, and unlike the 105 corpus generators that check runs on
// any clone, because its source is this repository rather than a deleted one.
//
// Optional (`?`) means the Go field carries `omitempty` and the key can be
// ABSENT. `| null` means the Go field is a pointer and the key is present
// carrying null. They are different things and the distinction is load-bearing:
// the dashboard's count setters test `!== undefined`, so an absent key renders an
// em dash while an explicit null renders the string "null".

export interface BandwidthDevice {
  srcIp: string;
  dstIp: string;
  rxMbps: number;
  txMbps: number;
  totalMbps: number;
  proto: string;
  iface: string;
  name: string;
  mac: string;
  country: string;
  city: string;
  org: string | null;
  cat: string | null;
  isLan: boolean;
  isIpv6: boolean;
}

export interface BandwidthPayload {
  ts: number;
  devices: BandwidthDevice[] | null;
  pollMs: number;
}

export interface Bridge {
  id: string;
  name: string;
  protocolMode: string;
  vlanFiltering: boolean;
  igmpSnooping: boolean;
  dhcpSnooping: boolean;
  fastForward: boolean;
  priority: string;
  ageingTime: string;
  macAddress: string;
  mtu: number | null;
  running: boolean;
  disabled: boolean;
  comment: string;
  portCount: number;
  rxMbps: number | null;
  txMbps: number | null;
}

export interface BridgePort {
  id: string;
  bridge: string;
  interface: string;
  pvid: number | null;
  role: string;
  edge: string;
  learn: string;
  horizon: string;
  pathCost: number | null;
  frameTypes: string;
  disabled: boolean;
  inactive: boolean;
  dynamic: boolean;
}

export interface BridgeHost {
  mac: string;
  onInterface: string;
  bridge: string;
  vid: number | null;
  dynamic: boolean;
  local: boolean;
  external: boolean;
  age: string;
}

export interface BridgesPayload {
  ts: number;
  pollMs: number;
  bridges: Bridge[] | null;
  ports: BridgePort[] | null;
  hosts: BridgeHost[] | null;
  hostTotal: number;
  hostCap: number;
  ratesAvailable: boolean;
  available: boolean;
  hostsAvailable: boolean;
}

export interface CapsManager {
  enabled: boolean;
  interfaces: string[] | null;
  caCertificate: string;
  certificate: string;
  requirePeerCertificate: boolean;
  upgradePolicy: string;
  packagePath: string;
}

export interface CapsCapMode {
  enabled: boolean;
  discoveryInterfaces: string[] | null;
  capsManAddresses: string[] | null;
  currentAddress: string;
  currentIdentity: string;
  certificate: string;
  slavesDatapath: string;
}

export interface CapsRadio {
  radioMac: string;
  interface: string;
  disabled: boolean;
}

export interface CapsClient {
  mac: string;
  interface: string;
  ssid: string;
  signal: number | null;
  uptime: string;
}

export interface Cap {
  identity: string;
  address: string;
  boardName: string;
  serial: string;
  version: string;
  baseMac: string;
  commonName: string;
  state: string;
  connectedTime: string;
  uptime: string;
  radios: CapsRadio[] | null;
  clients: CapsClient[] | null;
  clientCount: number;
}

export interface CapsProvisioning {
  id: string;
  identity: string;
  supportedBands: string[] | null;
  action: string;
  masterConfiguration: string;
  slaveConfigurations: string[] | null;
  nameFormat: string;
  radioMac: string;
  identityRegexp: string;
  comment: string;
  disabled: boolean;
}

export interface CapsTotals {
  caps: number;
  capsOk: number;
  radios: number;
  clients: number;
  clientsOnCaps: number;
  clientsLocal: number;
}

export interface CapsConfigProfile {
  id: string;
  name: string;
  ssid: string;
  mode: string;
  country: string;
  hideSsid: boolean;
  security: string;
  channel: string;
  datapath: string;
  manager: string;
  comment: string;
  disabled: boolean;
}

export interface CapsSecurityProfile {
  id: string;
  name: string;
  authTypes: string;
  wps: string;
  ft: boolean;
  comment: string;
  disabled: boolean;
}

export interface CapsChannelProfile {
  id: string;
  name: string;
  band: string;
  frequency: string;
  width: string;
  secondaryFrequency: string;
  skipDfsChannels: string;
  comment: string;
  disabled: boolean;
}

export interface CapsDatapathProfile {
  id: string;
  name: string;
  bridge: string;
  vlanId: string;
  clientIsolation: boolean;
  localForwarding: boolean;
  trafficProcessing: string;
  comment: string;
  disabled: boolean;
}

export interface CapsProfiles {
  configuration: CapsConfigProfile[] | null;
  security: CapsSecurityProfile[] | null;
  channel: CapsChannelProfile[] | null;
  datapath: CapsDatapathProfile[] | null;
}

export interface CapsmanPayload {
  ts: number;
  pollMs: number;
  role: string;
  manager: CapsManager;
  cap: CapsCapMode;
  caps: Cap[] | null;
  provisioning: CapsProvisioning[] | null;
  localRadios: CapsRadio[] | null;
  totals: CapsTotals;
  profiles: CapsProfiles;
  available: boolean;
}

export interface ConnProtoCounts {
  tcp: number;
  udp: number;
  icmp: number;
  other: number;
}

export interface ConnSource {
  ip: string;
  name: string;
  mac: string;
  count: number;
}

export interface ConnCountryProto {
  tcp: number;
  udp: number;
  other: number;
}

export interface ConnDestination {
  key: string;
  count: number;
  country: string;
  city: string;
  proto: ConnCountryProto;
  org: string | null;
  cat: string | null;
}

export interface ConnOrgCount {
  org: string;
  count: number;
  cat: string | null;
}

export interface ConnCountry {
  cc: string;
  city: string;
  count: number;
  proto: ConnCountryProto;
  orgs: ConnOrgCount[] | null;
}

export interface ConnPort {
  port: string;
  count: number;
}

export interface ConnDestEntry {
  key: string;
  count: number;
  country: string;
  city: string;
  org: string | null;
  cat: string | null;
}

export interface ConnsPayload {
  ts?: number;
  total: number;
  processed: number;
  processingCapped: boolean;
  newSinceLast?: number;
  protoCounts: ConnProtoCounts;
  topSources: ConnSource[] | null;
  topDestinations: ConnDestination[] | null;
  topCountries: ConnCountry[] | null;
  topPorts: ConnPort[] | null;
  countryDests: Record<string, ConnDestEntry[] | null> | null;
  countryPorts: Record<string, ConnPort[] | null> | null;
  sourceDests: Record<string, ConnDestEntry[] | null> | null;
  sourcePorts: Record<string, ConnPort[] | null> | null;
  pollMs: number;
}

export interface DNSSettings {
  servers: string[] | null;
  dynamicServers: string[] | null;
  dohEnabled: boolean;
  dohUrl: string;
  dohVerifyCert: boolean;
  dohMaxServerConnections: number | null;
  dohMaxConcurrentQueries: number | null;
  dohTimeout: string;
  allowRemoteRequests: boolean;
  cacheSize: number | null;
  cacheUsed: number | null;
  cacheMaxTtl: string;
  maxUdpPacketSize: number | null;
  maxConcurrentQueries: number | null;
  queryServerTimeout: string;
  queryTotalTimeout: string;
  mdnsRepeatIfaces: string[] | null;
  vrf: string;
}

export interface DNSStaticEntry {
  id: string;
  name: string;
  regexp: string;
  address: string;
  type: string;
  ttl: string;
  disabled: boolean;
  comment: string;
}

export interface DNSPayload {
  ts: number;
  pollMs: number;
  settings: DNSSettings;
  staticEntries: DNSStaticEntry[] | null;
  available: boolean;
}

export interface FirewallRule {
  id: string;
  chain: string;
  action: string;
  comment: string;
  srcAddress: string;
  dstAddress: string;
  protocol: string;
  dstPort: string;
  inInterface: string;
  packets: number;
  bytes: number;
  deltaPackets: number;
  disabled: boolean;
  dynamic: boolean;
}

export interface FirewallPayload {
  ts: number;
  filter: FirewallRule[] | null;
  nat: FirewallRule[] | null;
  mangle: FirewallRule[] | null;
  raw: FirewallRule[] | null;
  activeTable: string;
  pollMs: number;
}

export interface IfName {
  name: string;
  running: boolean;
  disabled: boolean;
}

export interface IfNamesPayload {
  ts: number;
  total: number;
  interfaces: IfName[] | null;
}

export interface Interface {
  name: string;
  type: string;
  running: boolean;
  disabled: boolean;
  comment: string;
  macAddr: string;
  rxMbps: number;
  txMbps: number;
  ips: string[] | null;
  rxBytes: number | null;
  txBytes: number | null;
  errors: number | null;
  drops: number | null;
  linkDowns: number | null;
  lastLinkUp: string;
  errorsDelta: number | null;
  dropsDelta: number | null;
  deltaWindowMs: number | null;
}

export interface IfStatusPayload {
  ts: number;
  routerId: string;
  interfaces: Interface[] | null;
}

export interface Network {
  cidr: string;
  gateway: string;
  dns: string;
  leaseCount: number;
  poolSize: number;
}

export interface InternetIface {
  name: string;
  ip: string;
}

export interface LanPayload {
  ts: number;
  lanCidrs: string[] | null;
  networks: Network[] | null;
  wanIp: string;
  totalPoolSize: number;
  totalLeases: number;
  pollMs: number;
  internetIfaces: InternetIface[] | null;
}

export interface Lease {
  ip: string;
  name: string;
  mac: string;
  hostName: string;
  comment: string;
  status: string;
  server: string;
  iface: string;
  vlanId: string;
  id: string;
  dynamic: boolean;
}

export interface LeaseServer {
  name: string;
  iface: string;
  vlanId: string;
  count: number;
}

export interface LeasesPayload {
  ts: number;
  leases: Lease[] | null;
  servers: LeaseServer[] | null;
}

export interface NetwatchHost {
  id: string;
  host: string;
  type: string;
  status: string;
  name: string;
  comment: string;
}

export interface NetwatchPayload {
  hosts: NetwatchHost[] | null;
  ts: number;
}

export interface PPPSession {
  id: string;
  name: string;
  service: string;
  address: string;
  callerId: string;
  uptime: string;
  encoding: string;
  sessionId: string;
  limitIn: number | null;
  limitOut: number | null;
  rx: number;
  tx: number;
  rxRate: number | null;
  txRate: number | null;
}

export interface PPPProfile {
  name: string;
  localAddress: string;
  remoteAddress: string;
  rateLimit: string;
  onlyOne: string;
  encryption: string;
}

export interface PPPServer {
  serviceName: string;
  interface: string;
  maxSessions: string;
  auth: string;
  disabled: boolean;
}

export interface PPPPayload {
  ts: number;
  pollMs: number;
  sessions: PPPSession[] | null;
  profiles: PPPProfile[] | null;
  servers: PPPServer[] | null;
  byService: Record<string, number> | null;
  totalRxRate: number | null;
  totalTxRate: number | null;
  available: boolean;
}

export interface Package {
  id: string;
  name: string;
  version: string;
  buildTime: string;
  size: number | null;
  scheduled: string;
  scheduledAction: string;
  disabled: boolean;
  onServer: boolean;
  state: string;
}

export interface Firmware {
  isRouterboard: boolean;
  boardName: string;
  model: string;
  serial: string;
  firmwareType: string;
  currentFirmware: string;
  upgradeFirmware: string;
  minimumFirmware: string;
  upgradeAvailable: boolean;
}

export interface Update {
  channel: string;
  installedVersion: string;
  latestVersion: string;
  status: string;
  updateAvailable: boolean;
}

export interface PackageCounts {
  total: number;
  installed: number;
  disabled: number;
  available: number;
  scheduled: number;
}

export interface PackagesPayload {
  ts: number;
  pollMs: number;
  packages: Package[] | null;
  firmware: Firmware;
  update: Update;
  counts: PackageCounts;
  pendingReboot: boolean;
  available: boolean;
}

export interface PingPayload {
  target: string;
  rtt: number | null;
  loss: number | null;
  minRtt?: number | null;
  maxRtt?: number | null;
  permissionDenied?: boolean;
  ts: number;
  pollMs: number;
}

export interface Pair {
  up: number | null;
  down: number | null;
}

export interface IntPair {
  up: number | null;
  down: number | null;
}

export interface RatePair {
  up: number | null;
  down: number | null;
}

export interface SimpleQueue {
  id: string;
  order: number;
  name: string;
  target: string;
  parent: string;
  packetMarks: string;
  priority: string;
  queueType: string;
  limitAt: Pair;
  maxLimit: Pair;
  burstLimit: Pair;
  bytes: IntPair;
  packets: IntPair;
  dropped: IntPair;
  queuedBytes: IntPair;
  disabled: boolean;
  invalid: boolean;
  dynamic: boolean;
  comment: string;
  rateBps: RatePair;
  rateSource: string | null;
  rateWindowMs: number | null;
}

export interface TreeQueue {
  id: string;
  order: number;
  name: string;
  parent: string;
  packetMark: string;
  priority: string;
  queueType: string;
  limitAt: number | null;
  maxLimit: number | null;
  burstLimit: number | null;
  bytes: number | null;
  packets: number | null;
  dropped: number | null;
  queuedBytes: number | null;
  disabled: boolean;
  invalid: boolean;
  dynamic: boolean;
  comment: string;
  fasttrackBypassable: boolean;
  rateBps: number | null;
  rateSource: string | null;
  rateWindowMs: number | null;
}

export interface Fasttrack {
  state: string;
  count: number;
  scoped: boolean;
}

export interface QueuesPayload {
  ts: number;
  pollMs: number;
  simple: SimpleQueue[] | null;
  tree: TreeQueue[] | null;
  fasttrack: Fasttrack;
  stats: string;
  available: boolean;
  denied: boolean;
}

export interface RosUser {
  id: string;
  name: string;
  group: string;
  address: string;
  comment: string;
  disabled: boolean;
  expired: boolean;
  lastLogin: string;
  inactivityTimeout: string;
  inactivityPolicy: string;
  protected: boolean;
}

export interface RosGroup {
  id: string;
  name: string;
  granted: string[] | null;
  denied: string[] | null;
  skin: string;
  comment: string;
  protected: boolean;
  members: number;
}

export interface RosSession {
  id: string;
  name: string;
  address: string;
  via: string;
  group: string;
  when: string;
  radius: boolean;
  protected: boolean;
}

export interface RosSelf {
  names: string[] | null;
  groups: string[] | null;
  resolved: boolean;
  source: string | null;
}

export interface RosPasswordPolicy {
  minLength: number;
  minCategories: number;
}

export interface RosUsersPayload {
  ts: number;
  pollMs: number;
  users: RosUser[] | null;
  groups: RosGroup[] | null;
  sessions: RosSession[] | null;
  self: RosSelf;
  passwordPolicy: RosPasswordPolicy;
  policies: string[] | null;
  available: boolean;
  denied: boolean;
}

export interface RouteCounts {
  total: number;
  connect: number;
  static: number;
  dynamic: number;
  bgp: number;
  ospf: number;
}

export interface Peer {
  key: string;
  peerType: string;
  name: string;
  description: string;
  remoteAddr: string;
  remoteAs: number;
  state: string;
  uptimeSec: number;
  prefixes: number;
  prefixHistory: number[] | null;
  updatesSent: number;
  updatesRecv: number;
  lastError: string;
  holdTime: number;
  keepalive: number;
  flapping: boolean;
}

export interface Route {
  dst: string;
  gateway: string;
  distance: number;
  active: boolean;
  comment: string;
  type: string;
  protocol: string;
  family: string;
  id: string;
}

export interface PeerSummary {
  total: number;
  established: number;
  down: number;
}

export interface RoutingPayload {
  ts: number;
  pollMs: number;
  routeCounts: RouteCounts;
  peers: Peer[] | null;
  routes: Route[] | null;
  summary: PeerSummary;
}

export interface SystemPayload {
  ts: number;
  uptimeRaw: string;
  cpuLoad: number;
  memPct: number;
  usedMem: number;
  totalMem: number;
  hddPct: number;
  totalHdd: number;
  freeHdd: number;
  version: string;
  latestVersion: string;
  updateAvailable: boolean;
  updateStatus: string;
  updateChannel: string;
  boardName: string;
  cpuCount: number;
  cpuFreq: number;
  tempC: number | null;
  pollMs: number;
  arch: string | null;
  serial: string | null;
  licenseLevel: string | null;
}

export interface TalkerDevice {
  name: string;
  mac: string;
  tx_mbps: number;
  rx_mbps: number;
}

export interface TalkersPayload {
  ts: number;
  devices: TalkerDevice[] | null;
  pollMs: number;
  available: boolean;
}

export interface TopoDiscovery {
  protocol: string[] | null;
  mode: string;
  interfaceList: string;
  interval: string;
}

export interface TopoVlan {
  vid: number;
  name: string;
}

export interface TopoEdge {
  id: string;
  from: string;
  to: string;
  iface: string;
  viaPort: string;
  remoteIface: string;
  shared: boolean;
  inferred: boolean;
  client?: boolean;
  gone: boolean;
}

export interface TopologyPayload {
  ts: number;
  routerId: string;
  pollMs: number;
  discovery: TopoDiscovery | null;
  permissionDenied: boolean;
  pingDenied: boolean;
  neighborCount: number;
  vlans: TopoVlan[] | null;
  clientCount: number;
  clientsTruncated: number;
  nodes: unknown[] | null;
  edges: TopoEdge[] | null;
}

export interface Tunnel {
  id: string;
  publicKey: string;
  type: string;
  name: string;
  state: string;
  comment: string;
  lastHandshake: string;
  keepalive: string;
  endpoint: string;
  allowedIp: string;
  interface: string;
  rx: number;
  tx: number;
  rxRate: number;
  txRate: number;
}

export interface PppTunnel {
  type: string;
  name: string;
  service: string;
  address: string;
  callerId: string;
  uptime: string;
  rx: number;
  tx: number;
}

export interface IpsecTunnel {
  type: string;
  name: string;
  state: string;
  uptime: string;
  side: string;
  enc: string;
  auth: string;
}

export interface VPNPayload {
  ts: number;
  tunnels: Tunnel[] | null;
  ppp: PppTunnel[] | null;
  ipsec: IpsecTunnel[] | null;
  pollMs: number;
}

export interface VlanInterface {
  id: string;
  name: string;
  parent: string;
  mtu: number | null;
  running: boolean;
  disabled: boolean;
  comment: string;
  rxMbps: number | null;
  txMbps: number | null;
}

export interface Vlan {
  vlanId: number;
  interfaces: VlanInterface[] | null;
  tagged: string[] | null;
  untagged: string[] | null;
  bridges: string[] | null;
  clients: number;
  rxMbps: number | null;
  txMbps: number | null;
  name: string;
}

export interface BridgeVlan {
  bridge: string;
  raw: string;
  ids: number[] | null;
  ranges: number[] | null[] | null;
  truncated: boolean;
  tagged: string[] | null;
  untagged: string[] | null;
  currentTagged: string[] | null;
  dynamic: boolean;
  disabled: boolean;
}

export interface VlanPort {
  bridge: string;
  interface: string;
  pvid: number | null;
  frameTypes: string;
  disabled: boolean;
}

export interface VlansPayload {
  ts: number;
  pollMs: number;
  vlans: Vlan[] | null;
  bridgeVlans: BridgeVlan[] | null;
  ports: VlanPort[] | null;
  dynamicCount: number;
  ratesAvailable: boolean;
}

export interface WANDhcp {
  id: string;
  status: string;
  server: string;
  primaryDns: string;
  secondaryDns: string;
  expiresAfter: string;
  invalid: boolean;
}

export interface WAN {
  name: string;
  type: string;
  isTunnel: boolean;
  state: string;
  since: string;
  running: boolean | null;
  address: string;
  isPublic: boolean | null;
  gateway: string;
  routeDistance: string;
  routeActive: boolean;
  hasDefaultRoute: boolean;
  rxMbps: number | null;
  txMbps: number | null;
  rxBytes: number | null;
  txBytes: number | null;
  dhcp: WANDhcp | null;
}

export interface WANPayload {
  ts: number;
  pollMs: number;
  wans: WAN[] | null;
  ratesAvailable: boolean;
  activeDefaultWan: string;
  publicIp: string;
  detectionEnabled: boolean;
  available: boolean;
  denied: boolean;
}

export interface WifiRadio {
  name: string;
  defaultName: string;
  mac: string;
  band: string;
  bandRaw: string;
  frequency: string;
  channelWidth: string;
  country: string;
  disabled: boolean;
  running: boolean;
  capsManaged: boolean;
  readOnlyReason: string;
  profile: string;
}

export interface WifiInherits {
  ssid: string | null;
  security: string | null;
  channel: string | null;
}

export interface WifiNetwork {
  id: string;
  name: string;
  ssid: string;
  radio: string;
  master: string;
  isVirtual: boolean;
  band: string;
  bandRaw: string;
  security: string;
  authTypes: string;
  hidden: boolean;
  vlanId: string;
  bridge: string;
  disabled: boolean;
  running: boolean;
  clients: number;
  comment: string;
  capsManaged: boolean;
  profile: string;
  profileUsedBy: number;
  inherits: WifiInherits | null;
  readOnlyReason: string;
  editable: boolean;
  removable: boolean;
  resource: string;
}

export interface WifiSecProfile {
  id: string;
  name: string;
  mode: string;
  authTypes: string;
  security: string;
  isDefault: boolean;
}

export interface WifiTotals {
  radios: number;
  networks: number;
  clients: number;
  capsManaged: number;
  readOnly: number;
}

export interface WifiPayload {
  ts: number;
  pollMs: number;
  stack: string;
  available: boolean;
  radios: WifiRadio[] | null;
  networks: WifiNetwork[] | null;
  secProfiles: WifiSecProfile[] | null;
  totals: WifiTotals;
}

export interface WirelessClient {
  mac: string;
  signal: number;
  iface: string;
  txRate: string;
  band: string;
  ip: string;
  rxRate: string;
  uptime: string;
  ssid: string;
  name: string;
  source?: string;
}

export interface WirelessSSID {
  ssid: string;
  ifaces: string[] | null;
  bands: string[] | null;
  disabled: boolean;
  running: boolean;
  clients: number;
}

export interface WirelessPayload {
  ts: number;
  clients: WirelessClient[] | null;
  mode: string;
  pollMs: number;
  capsmanAvailable: boolean;
  ssids: WirelessSSID[] | null;
  ssidsManagedElsewhere: number;
}

// The `conn:update` payload, which is NOT ConnsPayload.
//
// internal/collect/connections.go marshals it through `connsLight`, which deletes
// 4 keys (countryDests, countryPorts, sourceDests, sourcePorts). The page room gets the full ConnsPayload; the dashboard card
// room gets this. Both are real, and a consumer of one must not be typed as the
// other -- the keys are ABSENT here, not null.
export interface ConnsUpdate {
  ts?: number;
  total: number;
  processed: number;
  processingCapped: boolean;
  newSinceLast?: number;
  protoCounts: ConnProtoCounts;
  topSources: ConnSource[] | null;
  topDestinations: ConnDestination[] | null;
  topCountries: ConnCountry[] | null;
  topPorts: ConnPort[] | null;
  pollMs: number;
}
